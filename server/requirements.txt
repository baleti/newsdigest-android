# Core server - always needed.
fastapi
uvicorn[standard]

# Client-side WebSocket connections to ai1's tts-stt-server (the /tts/stream
# and /stt/stream proxy routes) - uvicorn[standard] already pulls this in
# for its own server-side use, but it's declared explicitly since this file
# now imports it directly too.
websockets
